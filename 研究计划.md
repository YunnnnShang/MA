# 总体研究目标（Main Objective）

本研究旨在构建一种基于 HEVC 软件编码器特征的能耗建模方法，用于精准估算 NVIDIA Jetson 平台 NVENC 硬件编码器在帧内编码模式下的系统级能耗。通过引入可解释的机器学习模型，本研究探索软件编码行为与黑盒硬件功耗之间的关联，为后续软硬件协同优化和低功耗视频处理系统设计提供理论与方法支撑。
搭建测量环境:
# 具体研究子目标（Sub-Objectives）

| 子目标编号  | 内容                                                                                   |
| ------ | ------------------------------------------------------------------------------------ |
| **O1** | 提取 x265 软件编码器中与帧内编码复杂度密切相关的特征，包括预测模式、CU划分结构、残差信息以及图像内容特征等，构建特征集合。                    |
| **O2** | 使用 Intel RAPL 和 LMG611 分别获取软件与硬件编码过程中精确的能耗数据，建立可复现的能耗采集平台。                           |
| **O3** | 采用 XGBoost 等机器学习方法，将软件特征映射至硬件能耗，构建精确的功耗估算模型，目标为误差率控制在 **5% 以内**。                     |
| **O4** | 比较多种回归模型（如 XGBoost、Linear Regression、MLP、SVM），并进行 ablation study，以验证各特征的重要性与模型性能敏感度。 |
| **O5** | 分析模型的可解释性，明确哪些编码行为对功耗影响最显著，辅助理解软硬件耦合机制。                                              |
| **O6** | 评估模型在不同 QP 值、分辨率、preset 参数和视频内容上的**泛化能力**与鲁棒性。                                       |
| **O7** | 可视化软硬件功耗随编码配置（如 QP、内容复杂度）变化的趋势，并分析它们之间的耦合/不一致性。                                      |

# 模型选择建议
| 模型                | 特点            | 适用建议           |
| ----------------- | ------------- | -------------- |
| **XGBoost**       | 精度高，解释性好      | 推荐 baseline    |
| **Random Forest** | 对特征冗余和非线性容忍度高 | 可作 baseline 对比 |
| **MLP** (多层感知机)   | 可建模复杂特征交互     | 特征量较多时可以试试     |
| **SVR**           | 小数据集时稳定       | 若样本量不多可作为对照组   |

# 评价指标（Evaluation Metrics）

| 指标类型      | 指标名称                      | 描述                          |
| --------- | ------------------------- | --------------------------- |
| **预测精度**  | MAE, MAPE, RMSE           | 用于评估模型预测能耗的精度，目标为 MAPE ≤ 5% |
| **泛化能力**  | Cross-domain Accuracy     | 评估模型对未见 QP、内容、preset 的预测能力  |
| **可解释性**  | SHAP / Feature Importance | 量化每个特征对模型输出的贡献              |
| **比较分析**  | 多模型对比                     | 验证不同模型的性能与复杂度 tradeoff      |
| **趋势一致性** | Pearson/Spearman 相关系数     | 衡量软件能耗趋势是否能解释硬件能耗变化         |

# 当前需求的目标特性

| 维度              | 说明                   |
| --------------- | -------------------- |
| **聚焦帧内编码**      | 避免干扰，聚焦 I 帧建模，任务范围清晰 |
| **公共数据集**       | 保证结果可复现，支持泛化分析       |
| **多参数建模**       | 支持多种 QP、preset，增强实用性 |
| **无需部署系统**      | 聚焦于建模与分析，暂不考虑部署复杂性   |
| **解释性强、模型不求复杂** | 精度优先，模型越简洁越好         |

软件端: 在你的x86 Linux系统上，准备好x265编码器。编写一个自动化脚本（Python或Bash），该脚本可以：

循环执行x265命令，遍历不同的视频序列、分辨率和配置（QP值、presets）。

在x265运行时，同步启动Intel RAPL的测量脚本来记录CPU和DRAM的能耗。

同时，使用perf stat -p <x265_pid> -e <event1>,<event2>...来记录关键的处理器事件。

确保使用x265 --csv参数将编码统计数据保存到文件中。

硬件端: 在Jetson平台上，准备好你的外部功率计（LMG611）。同样编写一个自动化脚本，可以：

调用ffmpeg或GStreamer框架来使用NVENC硬件编码器进行编码。

同步触发LMG611开始和停止测量，并将数据记录下来。

进行配置匹配研究:

仔细阅读我为你提供的x265和NVENC文档，特别是关于帧内编码（Intra-coding）和性能/质量权衡（preset/tuning）的部分。

制作一张参数映射表，尝试将x265的关键帧内编码参数与NVENC的API选项对应起来。

数据收集与模型训练:

在两个平台上运行你定义的匹配配置，收集所有数据（软件能耗、PEs、编码统计、硬件能耗）。

将数据对齐（例如，按帧或按视频序列）。

从最简单的线性回归模型开始，复现论文中的思想，用你的x265测量数据作为输入，硬件能耗作为输出来训练模型，并评估其准确性。
